# VITE_API_URL is passed as a Docker build arg (baked in at build time for browser calls)
# INTERNAL_URL is runtime — SvelteKit SSR calls backend via Docker internal network
INTERNAL_URL=http://backend:8080
