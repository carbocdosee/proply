# Go backend URL (internal Railway URL in production)
VITE_API_URL=http://localhost:8080

# Internal URL for server-side SvelteKit → Go calls (not exposed to browser)
INTERNAL_URL=http://localhost:8080
